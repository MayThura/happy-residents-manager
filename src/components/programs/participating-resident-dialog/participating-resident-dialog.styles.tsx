import Avatar from '@mui/material/Avatar';
import List from '@mui/material/List';
import ListItem from '@mui/material/ListItem';
import ListItemAvatar from '@mui/material/ListItemAvatar';
import ListItemButton from '@mui/material/ListItemButton';
import ListItemText from '@mui/material/ListItemText';
import DialogTitle from '@mui/material/DialogTitle';
import Dialog from '@mui/material/Dialog';
import PersonIcon from '@mui/icons-material/Person';
import AddIcon from '@mui/icons-material/Add';
import { styled } from '@mui/material/styles';

export const ResidentDialogBox = styled(Dialog)``;

export const ResidentDialogTitle = styled(DialogTitle)``;

export const ResidentList = styled(List)``;

export const ResidentListItem = styled(ListItem)``;

export const ResidentListItemAvatar = styled(ListItemAvatar)``;

export const ResidentListItemButton = styled(ListItemButton)``;

export const ResidentListItemText = styled(ListItemText)`
    padding-right: 15px;
`;

export const ResidentIcon = styled(PersonIcon)``;

export const ResidentAddIcon = styled(AddIcon)``;

export const ResidentAvatar = styled(Avatar)``;