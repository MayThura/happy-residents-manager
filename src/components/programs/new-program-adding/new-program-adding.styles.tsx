import { styled } from '@mui/material/styles';
import TextField from '@mui/material/TextField';
import DialogActions from '@mui/material/DialogActions';
import DialogContent from '@mui/material/DialogContent';
import DialogContentText from '@mui/material/DialogContentText';
import Button from '@mui/material/Button';
import Autocomplete from '@mui/material/Autocomplete';
import FormGroup from '@mui/material/FormGroup';
import FormControlLabel from '@mui/material/FormControlLabel';
import FormLabel from '@mui/material/FormLabel';
import Checkbox from '@mui/material/Checkbox';

export const DialogInputField = styled(TextField)``;

export const NewDialogActions = styled(DialogActions)``;

export const NewDialogContent = styled(DialogContent)``;

export const NewDialogContentText = styled(DialogContentText)``;

export const DialogButton = styled(Button)``;

export const DialogAutocomplete = styled(Autocomplete)``;

export const DialogFormGroup = styled(FormGroup)`
    margin-top: 15px;
`;

export const DialogFormControlLabel = styled(FormControlLabel)``;

export const DialogFormLabel = styled(FormLabel)``;

export const DialogCheckbox = styled(Checkbox)``;