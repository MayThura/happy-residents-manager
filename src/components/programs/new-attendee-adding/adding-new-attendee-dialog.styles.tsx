import { styled } from '@mui/material/styles';
import DialogTitle from '@mui/material/DialogTitle';
import Dialog from '@mui/material/Dialog';
import DialogActions from '@mui/material/DialogActions';
import DialogContent from '@mui/material/DialogContent';
import DialogContentText from '@mui/material/DialogContentText';
import Button from '@mui/material/Button';
import TextField from '@mui/material/TextField';
import Autocomplete from '@mui/material/Autocomplete';

export const AddDialog = styled(Dialog)``;

export const AddDialogTitle = styled(DialogTitle)``;

export const AddDialogAction = styled(DialogActions)``;

export const AddDialogContent = styled(DialogContent)``;

export const AddDialogContentText = styled(DialogContentText)``;

export const DialogButton = styled(Button)``;

export const AddDialogInput = styled(TextField)``;

export const DialogAutocomplete = styled(Autocomplete)``;